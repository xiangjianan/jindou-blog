---
// Note: Starlight 0.38 doesn't support Page component override - this file is a no-op.
// Custom scripts are loaded via head config in astro.config.mjs instead.
import Default from '@astrojs/starlight/Page';
import MermaidScript from './MermaidScript.astro';
---

<Default {...Astro.props} />

<MermaidScript />
